import React from 'react'
import {
  Filters,
  Modal,
} from '../../Components'

const FilterModal = (
) => {
  return( 
    <Modal show={true}>
      <Filters
        
      />
    </Modal>
  )
}

export default FilterModal
